import 'package:flutter/material.dart';

class CartProductHeader extends StatelessWidget {
  const CartProductHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
